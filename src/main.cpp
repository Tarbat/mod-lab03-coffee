// Copyright 2022 GHA Test Team
#include "Automata.h"

int main() {
    Automata automata;
    automata.on();
    automata.getMenu();
    automata.coin(20);
    automata.choice(2);
    automata.check();
    automata.cook();
    automata.finish();
    automata.off();
    return 0;
}
